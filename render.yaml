services:
  - type: web
    name: hijab-fastapi-backend
    env: python
    rootDir: backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
